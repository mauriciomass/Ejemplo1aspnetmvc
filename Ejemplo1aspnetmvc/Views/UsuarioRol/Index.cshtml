@model IEnumerable<Ejemplo1aspnetmvc.Models.usuariorol>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            UsuarioRol
        </th>
        <th>
            Rol
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Ejemplo1aspnetmvc.Controllers.UsuarioRolController.NombreUsuarioRol(item.idUsuario)
            
        </td>
        <td>
              @Ejemplo1aspnetmvc.Controllers.UsuarioRolController.NombreRol(item.idRol)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
